# jsonviewer
An easy way to view and edit JSON
Make sure to install all the necessary NPM modules ('npm install') and Bower modules ('bower install') then simply run 'gulp' to have a live reload setup and running.